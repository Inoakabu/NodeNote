extends layout

block content
  h1= title
  p Welcome to #{title}

  if error
    p Error getting dynamic content.
  else
    p Counts:

    ul
      li #[strong Notes:] !{data.Model_count}

    <!-- Input from site -->    
    <form method="post" action="/">
      <input type="text" name="user[name]">
    </form>